// Dependencies
const PATH = require('./paths');

const ALIASES = {
  DATA: PATH.DATA_DIR,
  SCSS: PATH.SCSS_DIR,
  CSS: PATH.CSS_DIR,
  FONTS: PATH.FONTS_DIR,
  IMG: PATH.IMG_DIR,
  JSON: PATH.JSON_DIR,
  ROOT: PATH.STATIC_DIR
};

module.exports = ALIASES;
